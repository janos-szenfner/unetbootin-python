# Core dependencies
# CustomTkinter: modern, HiDPI-aware widgets on top of Tkinter. Replaces
# PySimpleGUI, whose licence terms have changed across major versions.
customtkinter>=6.0.0
# Pillow is used for the window/category icons (CTkImage).
Pillow>=10.0.0
requests>=2.28.0
py7zr>=0.20.0
psutil>=5.9.0

# Optional dependencies
pyudev>=0.24.0 ; sys_platform == 'linux'
pyobjc>=9.0 ; sys_platform == 'darwin'
pywin32>=305 ; sys_platform == 'win32'

# Development dependencies
pytest>=7.0.0
black>=23.0.0
mypy>=1.0.0
setuptools>=61.0.0
