# Core dependencies
# PySimpleGUI 6.2 is released under the GPLv3 (a free copyleft license,
# compatible with this project's GPLv2-or-later). Pinned to avoid pulling the
# withdrawn commercial-license-key 5.x line.
PySimpleGUI==6.2
requests>=2.28.0
py7zr>=0.20.0
psutil>=5.9.0

# Optional dependencies
pyudev>=0.24.0 ; sys_platform == 'linux'
pyobjc>=9.0 ; sys_platform == 'darwin'
pywin32>=305 ; sys_platform == 'win32'

# Development dependencies
pytest>=7.0.0
black>=23.0.0
mypy>=1.0.0
setuptools>=61.0.0
